# frontend

Vite + React + Tailwind static site. Fetches `/data/cards.json` (generated by `../pipeline/`) at the site root — no backend, no env vars needed.

```
npm install
npm run dev     # http://localhost:5173
npm run build   # -> dist/
```

See the repo root `README.md` for the full data pipeline / architecture.
